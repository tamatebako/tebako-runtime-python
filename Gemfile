# frozen_string_literal: true

source "https://rubygems.org"

# contract.yml schema validation (scripts/check_contract.rb).
gem "json_schemer", "~> 2.4"

# The spec suite (boot-contract parity, matrix planner, builder model)
# arrives with the real build machinery — rspec/rubocop join this file
# in the same PR (tebako-runtime-ruby's Gemfile is the model).
