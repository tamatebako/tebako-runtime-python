# macOS host build dependencies for the macos legs (CI).
# The exact set settles with the first real build leg (TODO.python/02);
# the entries below are what TODO.python/01's relocation probe needed for
# a stdlib+ssl CPython build on macos-arm64.
brew "openssl@3"
brew "pkg-config"
brew "xz"
